import * as React from "react";
import * as ReactDOM from 'react-dom'
import DawaAutocomplete2 from '.'

describe('DawaAutocomplete2', () => {
  it('is truthy', () => {
    expect(DawaAutocomplete2).toBeTruthy()
  });
  // it('renders without crashing', () => {
  //   const div = document.createElement('div');
  //   ReactDOM.render(<DawaAutocomplete2 select={(selected) => {console.log(selected)}}/>, div);
  //   ReactDOM.unmountComponentAtNode(div);
  // });
})
